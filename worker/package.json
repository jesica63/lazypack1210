{
  "name": "seo-lazypack-api-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker API proxy for SEO LazyPack",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "private": true
}
